apiVersion: v2
name: prometheus-adapter
description: Wrapper chart to install prometheus-adapter with provided adapter rules
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: prometheus-adapter
    version: "4.14.0"
    repository: "https://prometheus-community.github.io/helm-charts"
    condition: adapter.enabled
